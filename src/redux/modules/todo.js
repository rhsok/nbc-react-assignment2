const initialState = [
  {
    title: "제목1",
    contents: "내용2",
    id: "id",
    isDone: true,
  },
];

const todo = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
